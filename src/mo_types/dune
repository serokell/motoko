(library
  (name mo_types)
  (libraries wasm lib lang_utils mo_config)
  (inline_tests)
  (flags (:standard -w -66))
  (preprocess (pps ppx_inline_test ppx_yojson_conv))
  (instrumentation (backend bisect_ppx --bisect-silent yes))
)
